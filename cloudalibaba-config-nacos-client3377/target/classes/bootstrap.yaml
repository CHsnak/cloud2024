spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        # nacos作为注册中心
        server-addr: localhost:8848
      config:
        file-extension: yaml
        # nacos作为配置中心
        server-addr: localhost:8848